/**
 * Function that checks if the number is less then zero
 * @param numberZeroArg - the number that is to be set
 * @returns return true if the number is less than zero, otherwise returns false
 */
export const lessThenZero = (numberZeroArg: number): boolean => {
  return numberZeroArg > 0 ? false : true
}
