/**
 * Function that checks if the numbers are equal
 * @param numOne - The first number of equal number
 * @param numTwo - The second number of equal number
 * @returns Returns true if the two parameters number are equal, otherwise returns false
 */

export const equalNumbers = (numOne: number, numTwo: number): boolean => {
  return numOne === numTwo ? true : false
}
