/**
 * Function that increments a number by 1
 * @param startingNumber - The starting number that is to be set
 * @returns the number incremented by 1
 */
export const incrementNumber = (startingNumber: number): number => {
  return startingNumber + 1
}
